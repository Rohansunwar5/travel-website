import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [ 'plus.unsplash.com', 'images.unsplash.com', 'assets.aceternity.com', 'cdn.rareblocks.xyz'],
  },
};

export default nextConfig;
